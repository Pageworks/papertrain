{% set globalCssAssets = craft.papertrain.getAssetPaths(['main', 'fonts', 'container']) %}

<script defer="defer">
    {% for asset in globalCssAssets %}
        window.stylesheets.push("{{ asset.css }}");
    {% endfor %}
</script>

