{% if block.selectedForm|length %}
    {% set form = craft.freeform.form(block.selectedForm.handle) %}
	<div class="o-freeform o-form {{ block.formStyle }} o-wrapper u-1/1" data-module="Freeform BasicForm">
        {{ form.render() }}
	</div>
{% endif %}
